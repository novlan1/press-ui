export declare let SUPPORT_PASSIVE: boolean;
export declare const passive: (passive?: boolean) => false | {
  passive: boolean;
};
export declare const PASSIVE_OPTIONS: boolean | {
  passive: boolean;
};
